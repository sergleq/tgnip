# Параллелизм в Telegram Bot

## 📊 Обзор архитектуры

Бот реализует **Worker Pool** паттерн для эффективной обработки множественных запросов от пользователей.

### Основные компоненты:

1. **Worker Pool** - пул горутин для параллельной обработки
2. **Task Queue** - буферизованный канал для задач
3. **Rate Limiting** - ограничение запросов к Telegram API
4. **Metrics** - сбор метрик производительности
5. **Configuration** - гибкая настройка параметров

## 🚀 Преимущества параллелизма

### До внедрения:
- ❌ Последовательная обработка сообщений
- ❌ Блокировка при медленных HTTP запросах
- ❌ Один пользователь блокирует всех остальных
- ❌ Нет контроля нагрузки

### После внедрения:
- ✅ Параллельная обработка до 20+ запросов
- ✅ Неблокирующие HTTP запросы
- ✅ Graceful handling перегрузки
- ✅ Мониторинг производительности

## ⚙️ Конфигурация

### Переменные окружения:

```bash
# Количество воркеров (5-20)
WORKER_COUNT=5

# Размер буфера очереди
QUEUE_BUFFER_SIZE=10

# Лимит запросов к Telegram API в секунду
RATE_LIMIT_PER_SEC=20

# Таймаут HTTP запросов в секундах
HTTP_TIMEOUT=30

# Уровень логирования
LOG_LEVEL=info

# Интервал логирования метрик в минутах
METRICS_INTERVAL=5
```

### Рекомендации по настройке:

#### Низкая нагрузка (до 10 пользователей):
```bash
WORKER_COUNT=3
QUEUE_BUFFER_SIZE=5
RATE_LIMIT_PER_SEC=10
```

#### Средняя нагрузка (10-50 пользователей):
```bash
WORKER_COUNT=8
QUEUE_BUFFER_SIZE=15
RATE_LIMIT_PER_SEC=20
```

#### Высокая нагрузка (50+ пользователей):
```bash
WORKER_COUNT=15
QUEUE_BUFFER_SIZE=30
RATE_LIMIT_PER_SEC=30
```

## 📈 Метрики производительности

Бот автоматически собирает и логирует метрики:

```
Метрики: Uptime=1h30m, Total=150, Active=3, Completed=145, Failed=2, Queue=2
```

### Показатели:
- **Uptime** - время работы бота
- **Total** - общее количество запросов
- **Active** - активные запросы в обработке
- **Completed** - успешно обработанные запросы
- **Failed** - неудачные запросы
- **Queue** - задачи в очереди

## 🔧 Техническая реализация

### Worker Pool:
```go
type WorkerPool struct {
    workers    int                    // Количество воркеров
    taskChan   chan MessageTask       // Канал задач
    wg         sync.WaitGroup         // WaitGroup для синхронизации
    bot        *tgbotapi.BotAPI       // Экземпляр бота
    metrics    *Metrics               // Метрики
    rateLimiter *time.Ticker          // Rate limiter
}
```

### Обработка задач:
1. **Submit** - добавление задачи в очередь
2. **Worker** - параллельная обработка задач
3. **Rate Limiting** - контроль частоты запросов
4. **Metrics** - сбор статистики

### Graceful Shutdown:
- Корректное завершение всех горутин
- Ожидание завершения активных задач
- Логирование статистики

## 🚨 Обработка перегрузки

При переполнении очереди:
1. Отправляется сообщение о перегрузке
2. Запрос не обрабатывается
3. Увеличивается счетчик неудачных запросов
4. Логируется предупреждение

### Сообщения:
- 🇷🇺 "⚠️ Сервер перегружен. Попробуйте позже."
- 🇺🇸 "⚠️ Server is overloaded. Please try again later."

## 📊 Производительность

### Теоретические возможности:
- **5 воркеров**: до 5 параллельных запросов
- **20 воркеров**: до 20 параллельных запросов
- **Rate limiting**: 20-30 запросов/сек к Telegram API

### Практические ограничения:
- Скорость интернета
- Производительность сервера
- Лимиты Telegram API
- Время обработки веб-страниц

## 🔍 Мониторинг

### Логи:
```
INFO[2025-08-13T13:21:00Z] Запущен пул из 5 воркеров
INFO[2025-08-13T13:21:00Z] Воркер 0 запущен
INFO[2025-08-13T13:21:05Z] Воркер 0 обрабатывает сообщение от пользователя 123456
```

### Метрики (каждые 5 минут):
```
INFO[2025-08-13T13:25:00Z] Метрики: Uptime=4m0s, Total=25, Active=2, Completed=23, Failed=0, Queue=0
```

## 🛠️ Устранение неполадок

### Высокая нагрузка:
1. Увеличить `WORKER_COUNT`
2. Увеличить `QUEUE_BUFFER_SIZE`
3. Проверить `RATE_LIMIT_PER_SEC`

### Медленная обработка:
1. Проверить `HTTP_TIMEOUT`
2. Увеличить `WORKER_COUNT`
3. Проверить скорость интернета

### Ошибки:
1. Проверить логи
2. Уменьшить `RATE_LIMIT_PER_SEC`
3. Проверить доступность Telegram API

## 🔮 Будущие улучшения

1. **Кластеризация** - несколько экземпляров бота
2. **База данных** - сохранение метрик
3. **Webhook** - вместо polling
4. **Кэширование** - для повторных запросов
5. **Health checks** - мониторинг состояния
