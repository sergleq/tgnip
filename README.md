# TGNIP - Telegram Bot для преобразования ссылок в Markdown

Telegram бот, который преобразовывает ссылки на веб-страницы в markdown файлы с очищенным содержимым. Бот автоматически извлекает основной текст, заголовки, автора и дату публикации, создавая чистые markdown документы.

## 🚀 Возможности

- **Извлечение контента** - автоматическое извлечение основного текста из веб-страниц
- **Сохранение структуры** - поддержка абзацев, заголовков и форматирования
- **Качественная конвертация** - использование html-to-markdown для профессионального форматирования
- **Блоки кода** - извлечение и сохранение фрагментов кода с автоматическим определением языка программирования
- **Цитаты** - поддержка блоков цитирования
- **Списки** - нумерованные и маркированные списки
- **Таблицы** - качественная конвертация HTML таблиц в markdown с правильным форматированием
- **Заголовки** - правильное определение уровней заголовков
- **Многоязычная поддержка** - автоматическое определение языка пользователя и локализация интерфейса
- **Очистка от рекламы** - удаление рекламных блоков, навигации и других ненужных элементов
- **Markdown форматирование** - создание структурированных markdown файлов
- **Метаинформация** - извлечение заголовка, автора, даты публикации
- **Автоматическое именование файлов** - генерация понятных имен файлов на основе URL

## 🛠 Технологии

- **Go 1.21+** - основной язык программирования
- **go-telegram-bot-api** - работа с Telegram Bot API
- **go-readability** - извлечение основного контента с сохранением структуры
- **html-to-markdown** - качественная конвертация HTML в markdown
- **goquery** - парсинг HTML и извлечение метаданных
- **logrus** - структурированное логирование
- **Docker** - контейнеризация приложения

## 📋 Требования

- Go 1.21 или выше
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

## 🚀 Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/tgnip.git
cd tgnip
```

### 2. Настройка переменных окружения

```bash
cp env.example .env
```

Отредактируйте файл `.env` и добавьте ваш токен бота:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
```

### 3. Установка зависимостей

```bash
go mod download
```

### 4. Запуск бота

```bash
go run .
```

## 🐳 Запуск с Docker

### Сборка и запуск

```bash
# Сборка образа
docker build -t tgnip-bot .

# Запуск контейнера
docker run -d \
  --name tgnip-bot \
  --env-file .env \
  tgnip-bot
```

### Использование Docker Compose

```bash
# Запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

## 📖 Использование

1. Найдите бота в Telegram по его username
2. Отправьте команду `/start` для начала работы
3. Отправьте ссылку на веб-страницу
4. Получите markdown файл с очищенным содержимым

### Пример использования

```
Пользователь: /start
Бот: Привет! Я бот для преобразования ссылок в markdown файлы...

Пользователь: https://example.com/article
Бот: ⏳ Обрабатываю ссылку...
Бот: [Отправляет файл article.md]
```

## 🌍 Поддерживаемые языки интерфейса

Бот автоматически определяет язык пользователя и предоставляет интерфейс на соответствующем языке:

- 🇷🇺 **Русский** (ru) - язык по умолчанию
- 🇺🇸 **Английский** (en, en-US)
- 🇪🇸 **Испанский** (es)
- 🇫🇷 **Французский** (fr)
- 🇩🇪 **Немецкий** (de)
- 🇮🇹 **Итальянский** (it)
- 🇵🇹 **Португальский** (pt)
- 🇨🇳 **Китайский** (zh)
- 🇯🇵 **Японский** (ja)

## 💻 Поддерживаемые языки программирования

Бот автоматически определяет язык программирования в блоках кода и создает markdown с правильной подсветкой синтаксиса:

- **JavaScript** (js, javascript, ecmascript)
- **Python** (python, py)
- **Java** (java)
- **C/C++** (c, cpp, c++, cplusplus)
- **C#** (c#, csharp, cs)
- **Go** (go, golang)
- **Rust** (rust, rs)
- **PHP** (php)
- **Ruby** (ruby, rb)
- **Swift** (swift)
- **Kotlin** (kotlin, kt)
- **TypeScript** (typescript, ts)
- **HTML** (html, htm)
- **CSS** (css)
- **SQL** (sql)
- **Shell** (bash, shell, sh, zsh)
- **JSON** (json)
- **XML** (xml)
- **YAML** (yaml, yml)
- **Markdown** (markdown, md)
- **Docker** (dockerfile, docker)
- **Git** (git)
- **Diff** (diff, patch)

### Форматы указания языка в HTML:
- `class="language-python"`
- `class="lang-javascript"`
- `class="go"`
- `class="highlight language-cpp"`

## 📁 Структура проекта

```
tgnip/
├── main.go                 # Основной файл бота
├── content_extractor.go    # Извлечение контента из веб-страниц
├── markdown_converter.go   # Конвертация в markdown
├── localization.go         # Система локализации
├── go.mod                  # Зависимости Go
├── Dockerfile              # Конфигурация Docker
├── docker-compose.yml      # Docker Compose конфигурация
├── .env                    # Переменные окружения
├── env.example             # Пример переменных окружения
└── README.md               # Документация
```

## 🔧 Конфигурация

### Переменные окружения

| Переменная | Описание | Обязательная |
|------------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | Да |
| `DEBUG` | Включить режим отладки | Нет |
| `LOG_LEVEL` | Уровень логирования | Нет |

### Настройка логирования

По умолчанию используется уровень `info`. Доступные уровни:
- `debug` - подробная отладочная информация
- `info` - основная информация (по умолчанию)
- `warn` - только предупреждения и ошибки
- `error` - только ошибки

## 🧪 Тестирование

```bash
# Запуск тестов
go test ./...

# Запуск тестов с покрытием
go test -cover ./...
```

## 📝 Логирование

Бот ведет подробные логи всех операций:

- Извлечение контента из URL
- Ошибки при обработке
- Успешные отправки файлов
- Статистика использования

## 🔒 Безопасность

- Проверка валидности URL
- Таймауты для HTTP запросов
- Экранирование специальных символов в markdown
- Логирование всех операций

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 🆘 Поддержка

Если у вас возникли проблемы или вопросы:

1. Проверьте [Issues](https://github.com/yourusername/tgnip/issues)
2. Создайте новое issue с подробным описанием проблемы
3. Убедитесь, что указали версию Go и операционную систему

## 🚀 Планы развития

- [ ] Поддержка JavaScript-рендеринга (Puppeteer/Playwright)
- [ ] Извлечение изображений
- [ ] Поддержка PDF экспорта
- [ ] Веб-интерфейс для настройки
- [ ] API для интеграции с другими сервисами
- [ ] Кэширование результатов
- [ ] Поддержка множественных форматов (HTML, TXT)

---

**Создано с ❤️ на Go** 